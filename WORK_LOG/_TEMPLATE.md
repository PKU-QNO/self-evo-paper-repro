# 复现日志：<论文短名> <arXiv/DOI> — <目标图/量> (v<N>)

> 一篇复现一文件（按论文分）。同一篇复现多次 → 复制本模板为 `-v2`/`-v3`（version 由用户第一句话给定）。
> case：`<MMDD-NN-papername-vN>` · 起始 <YYYY-MM-DD>
> **永不归档、永不删减；只增不改历史。** 每个会话/报告点结束前**追加**一段带日期条目，不覆盖旧条目。
> 与 `todo.md` 分工：todo 记待办/状态（临时、迭代后归档）；本文件记叙事+决策（永久）。数据只**引用** capsule/run_manifest/.result 路径，不重抄。

## 0. 本篇速览（可随进展更新的唯一"活"区块，其余只增不改）

- 目标图/量：
- 公式主源 / 交叉验证源：
- result_class 当前：`not_run`
- 当前停在：
- 关键产物路径：`.work/.todo/<paper>/<case>/`、`.result/<paper>/`、`.work/.result/<case>/capsule.md`

---

## 1. 分批次详细记录（只增不改，按日期追加）

### <stepXX + GateN>（YYYY-MM-DD）
- 做了什么：
- 关键决策 + 为什么：
- 产物（引用路径，不重抄数据）：
- verifier / result_class：
- 下一步 / 停机点：

<!-- 下一个会话/报告点在此下方继续追加，不要修改上面的条目 -->

---

## 2. 决策台账（只增不改；读此即可恢复决策，不必翻对话）

> 每条：**CC 建议** → **用户裁决** → **落点**（文件 + memento id）。

### D-01 <决策名>（YYYY-MM-DD）
- 背景：
- CC 建议：
- 用户裁决：
- 落点：

---

## 3. 待回看 / 可推翻项（已落、不阻塞，等有空再定）

- （SEPR 自定阈值、暂定策略、软约束残留等标这里，注明"已按默认落，可推翻"）
