# 工作报告 — 01-pdf_preprocessing — Akimov 2401.04146

## 固定头 6 字段

- **role**：sub-agent（W-sub，复现执行者），被 main-agent spawn 执行第 01 步 `pdf_preprocessing`。非编排者。
- **task_scope**：只执行 01-pdf_preprocessing，把 Akimov 论文 PDF/LaTeX 源转成结构化文本（正文/公式/图/表分离），不替 main-agent 决定 workflow 走向。
- **evidence_refs**：
  - LaTeX 源（权威公式来源）：`.work/.todo/2401.04146/0703-01-akimov-mie-v1/01-pdf_preprocessing/src/Text-rev.tex`（664 行）
  - 原始 PDF（9 页电子版，交叉核对）：`.paper/mie/2401.04146.pdf`
  - 产物：`.work/.todo/2401.04146/0703-01-akimov-mie-v1/` 下 `paper_text.md`、`formulas.md`、`figures.md`、`tables.md`、`figs/Fig1.png`–`figs/Fig12.png`（12 张，3x 渲染）
- **confidence**：高（公式/图 caption 来自 LaTeX 源逐字提取，非 OCR；坐标轴由渲染图人工读取）。唯一不确定点见下方 uncertainty。
- **blocked_by**：无（本步未 blocked）。但存在一个需 main-agent 上抛用户的**计划-论文不符**问题，见 recommended_action。
- **recommended_action**：
  1. **【须找用户重定目标图】关键发现：Akimov 2401.04146 论文中不存在经典的 $Q_{ext}(x)$（或 $Q_{sca}(x)$）vs 尺寸参数、跨 Rayleigh→Mie→几何光学过渡的那张曲线图。** 而 FINAL 计划/执行手册假设阶段1要复现这张图。计划与论文实际内容不符。请 main-agent 停下来找用户重新指定 step08 目标图。
  2. 建议在 `figures.md` 末尾给出的 4 候选（A=Fig3 超辐射/非辐射 loci、B=Fig4 Ag/Si 大球散射谱、C=Fig5(c,f) $|a_1|,|b_1|$ 曲线、D=Fig6 超吸收 loci）中让用户拍板。执行者推荐 **A（零外部依赖、纯解析、可硬约束验证）** 或 **C（贴近实际光谱，但须先定 Ag/Si/SiO2 光学常数来源）**。
  3. 可进入 step02（读图抽参/公式核对），公式清单已就绪，$a_l,b_l,\sigma_{sca},\sigma_{abs}$ 完整。

---

## 主体 8 字段

### 1. 身份声明
我是 sub-agent（复现执行者 W-sub），由 main-agent spawn 执行 SEPR 首次真实论文复现的第 01 步 pdf_preprocessing。我不决定 workflow 走向、不碰 `.result/`、不改 `.claude/skills/`、不宣布物理复现成功。

### 2. 做了什么
1. 加载 skill 列表（`skill-print.py`）、身份 skill（sub-agent）、本步 workflow skill（01-pdf_preprocessing）、pdf skill（确认是骨架、无预制脚本）。
2. 解压 LaTeX 源 `2401.04146_src.tar.gz` → 得主文件 `Text-rev.tex`（664 行）+ 12 张矢量图 `Fig{N}-eps-converted-to.pdf`。
3. 用 pymupdf 3x 渲染 12 张图为 PNG 至 `figs/`；Read 查看 Fig1/3/4/5 确认坐标轴与内容类型。
4. 确认原始 PDF 为 9 页电子版、有嵌入文本（page1 4646 字符），**无需 OCR**。
5. 从 LaTeX 源逐条转写产出 4 个结构化 Markdown：`paper_text.md`（分节正文）、`formulas.md`（公式清单含上下文+LaTeX+复现注意）、`figures.md`（12 图 caption+分类+坐标轴+可复现性+step08 候选表）、`tables.md`（占位，Grep 核实无表格）。

### 3. 用了什么工具
- `Bash`：tar 解压、pymupdf 渲染与文本核对、目录创建
- `Read`：读 tex 源、查看渲染图（Fig1/3/4/5）
- `Grep`：核实无 `\begin{table}`
- `Write`：写 4 产物 + 本报告
- Python 库：`fitz`（pymupdf 1.27.2.3）
- **未用**：OCR（不需要）、subsubagent（本步机械提取无需拆分，未 spawn sub-leaf）、memento MCP（不可用，见 memory_search_summary）

### 4. 遇到什么问题
- **Windows tar 路径**：`C:\...` 被 tar 误解析为远程主机，改用 `/c/...` unix 路径解决。
- **图有 trim 留白**：多张图（Fig1/3/6/9-12）eps 页含大片底部留白（includegraphics `trim` 参数导致），渲染出的 PNG 上半是图、下半空白——不影响内容，图在顶部完整。
- **memento MCP 不可用**：memory_search/memory_store 无对应工具（ToolSearch 已确认不可用），用文件系统 `.work/memento-cache/`（当前为空）兜底。
- **无 blocker**：提取全部干净完成。

### 5. 结果（产物路径 + 关键数值 + 验证状态）
- 产物（case 工作区根 `.work/.todo/2401.04146/0703-01-akimov-mie-v1/`）：
  - `paper_text.md` — 分节正文（Abstract/Intro/Mie theory/Features/Limitations/Conclusion）
  - `formulas.md` — 公式清单，含 ★ 核心：Mie 系数 $a_l,b_l,c_l,d_l$（S:a_l–S:d_l）、截面 $\sigma_{sca},\sigma_{abs}$、Riccati-Bessel $\psi_l(q)=q j_l(q)$/$\xi_l(q)=q h_l^{(1)}(q)$、$q_i=k_iR$/$q_e=k_eR$、两类场分解 $a_l=a_l^{(1)}+a_l^{(2)}$、极限态 $\sigma^{sr}/\sigma^{sa}$、Rayleigh $|a_1|\propto q_e^3$
  - `figures.md` — 12 图清单 + step08 目标图 4 候选表
  - `tables.md` — 占位（论文无表）
  - `figs/Fig1.png`–`Fig12.png` — 12 张数据图（3x 渲染）
- 中间产物：`.work/.todo/2401.04146/0703-01-akimov-mie-v1/01-pdf_preprocessing/src/`（tex 源 + 12 fig pdf）
- 关键数值/事实（逐字保留）：
  - 尺寸参数记号 $q_e=k_eR$（等价常见 $x$）；相对折射率 $m=\sqrt{\varepsilon_i/\varepsilon_e}$，$q_i=mq_e$
  - 图归一化 $\sigma/(\pi R^2)=Q$；超辐射极限归一化 $=2(2l+1)/q_e^2$（Fig4/5/7/8 虚线）
  - 材料/尺寸：Fig4 Ag R=300nm/Si R=200nm；Fig5/8 Ag R=25nm/Si R=40nm；均嵌 SiO2；谱 $\hbar\omega$ 0–6 eV；Ag 在 $\hbar\omega\approx3.9$ eV 有 $\varepsilon_i=\varepsilon_e$ 非辐射谷
- 验证状态：**仅文本提取，无物理验证**（本步不涉及仿真/verifier）。

### 6. 决策性回答 ★（回答 main-agent 的 3 个决策问题）

**决策问题1：PDF 是扫描版还是电子版？需不需要 OCR？**
- 答：**电子版**。9 页，有嵌入文本层（page1 4646 字符）。**不需要 OCR**。且拿到了 arXiv LaTeX 源，公式/caption 直接从源逐字取，比 PDF 文本流更准更干净——本步公式与 caption 全部以 LaTeX 源为准。
- uncertainty：无。missing_evidence：none。

**决策问题2：公式能不能干净提取，还是需要图片化？**
- 答：**能干净提取，无需图片化**。LaTeX 源公式完整、可读、可复现。核心公式（$a_l,b_l$、$\sigma_{sca}/\sigma_{abs}$、Riccati-Bessel、极限态）已全部转写进 `formulas.md`。
- uncertainty：本文 $a_l/b_l$ 写法带显式 $q_i,q_e$ 因子，与 Bohren-Huffman (BH) 标准记号形式不同但等价；step06 实现时建议以 BH 标准式为准并用本文式交叉验证，注意 $\psi_l',\xi_l'$ 是对各自宗量求导。
- missing_evidence：本文未给 $\psi_l'$ 递推式（可从 BH/scipy 特殊函数补，非阻塞）。

**决策问题3：有没有数据图需要数字化？具体哪张、画什么物理量 vs 什么自变量、什么条件（m/材料）？—— 决定 step08 复现哪张图。**
- 答：**有数据图，但没有计划假设的那张经典 $Q_{ext}(x)$ vs 尺寸参数曲线**（这是关键发现，见下）。可数字化的候选如下（复述自 `figures.md` 末尾）：

  | 候选 | 图 | 画的量 | 自变量 | 条件 | 需外部材料数据？ | 复现难度 |
  |------|----|-------|--------|------|----------------|---------|
  | **A** | Fig3 | 超辐射(虚线)/非辐射(实线) loci | $(q_e,\varepsilon_i/\varepsilon_e)$ 平面 | 纯实 $\varepsilon$，$l=1,2,3$，TM/TE | **否** | 低（解析求根 $a_l=1$ 或 $0$） |
  | B | Fig4 | $\sigma_{sca}/(\pi R^2)$ | $\hbar\omega$ 0–6 eV | Ag R=300nm / Si R=200nm @SiO2 | **是**（Ag/Si/SiO2 色散） | 中 |
  | C | Fig5(c,f) | $\lvert a_1\rvert,\lvert b_1\rvert$ | $\hbar\omega$ 0–6 eV | Ag R=25nm / Si R=40nm @SiO2 | **是** | 中 |
  | D | Fig6 | 超吸收 loci | $(q_e,\varepsilon_i/\varepsilon_e)$ | 复 $\varepsilon$ 优化（$a_l=1/2$） | 否 | 中（复根） |

- **推荐**：
  - 若求"零外部依赖、纯物理自洽、可硬约束验证"→ **候选 A（Fig3）**：只用本文解析式，可用 $a_l=1\Rightarrow\sigma_{sca,l}=\sigma^{sr}$ 做硬约束。
  - 若求"贴近实际光谱、对色散"→ **候选 C（Fig5 c/f）**：直接对比 Mie 系数模 $|a_1|,|b_1|$，最能验证 Mie 系数实现正确性，但须先确定 Ag/Si/SiO2 光学常数来源。
- **uncertainty（务必上抛）**：候选 B/C/D 若选，Ag/Si/SiO2 的 $\varepsilon(\omega)$ 论文未给数值，须用外部光学常数库（Ag: Johnson&Christy；Si: Green/Aspnes；SiO2: Malitson），与作者所用色散源不一致会影响吻合度。
- **missing_evidence**：（a）计划要求的经典 $Q_{ext}(x)$ 曲线在本论文不存在；（b）候选 B/C/D 所需材料光学常数数值。

### 7. 下一步需要的输入（接力信息）
- **给 main-agent / 用户**：拍板 step08 目标图（A/B/C/D）。这是继续 workflow 的前置。
- **若选 B/C/D**：需用户/main-agent 指定 Ag、Si、SiO2 的光学常数数据源（否则 step06/08 无法唯一确定曲线）。
- **给 step02（读图抽参）**：核心公式已在 `formulas.md`；建议 step02 用 scipy.special 的 Riccati-Bessel 实现，并以 BH 标准式交叉验证本文 $a_l,b_l$。
- **给 step06/07（建模/verifier）**：可用极限态做 verifier —— $a_l=1$→$\sigma^{sr}_{sca,l}=2\pi(2l+1)/k_e^2$；Rayleigh $q_e\ll1$→$|a_1|\propto q_e^3$；能量守恒 $\sigma_{ext}=\sigma_{sca}+\sigma_{abs}\ge0$。

### 8. 长期记忆更新
memento MCP 不可用（见 memory_search_summary），本次关键事实以文件系统兜底记录于本报告与产物。拟存记忆条目（供 main-agent 或后续接入 memento 时写入）：
1. Akimov 2401.04146 是理论综述，**无经典 $Q_{ext}(x)$ vs 尺寸参数曲线**；其数据图为 $(q_e,\varepsilon_i/\varepsilon_e)$ 平面极限态 loci（Fig3/6）与 Ag/Si 球光谱（Fig4/5/7/8，需外部色散）。
2. 该文记号 $q_e=k_eR$（尺寸参数）、$q_i=k_iR$，$a_l/b_l$ 分子分母带显式 $q$ 因子，与 BH 记号等价；复现须交叉验证。
3. arXiv 有 LaTeX 源时，公式/caption 优先从源取，比 OCR/PDF 文本流准得多——本次全程未用 OCR。
4. 踩坑：Windows Git Bash 下 tar 解压 `C:\...` 会被当远程主机，须用 `/c/...`。

---

## memory_search_summary
- **memento MCP 可用性**：**不可用**。ToolSearch 工具已确认不可用（spawn 指令告知），memory_search/memory_store 等 memento MCP 工具在本 session 未注册/不可调用。已按预案改用文件系统 `.work/memento-cache/` 兜底——该目录当前为空（无历史记忆）。
- **搜索结果**：无（工具不可用 + 缓存为空），故无命中记忆可复用。这是 SEPR 首次实跑，属预期。
- **如实标注**：本条为首次实跑收集的框架问题之一：子 agent session 无 memento 自动注入且 MCP 未接入，长期记忆暂靠报告 + 文件系统承载。

---

## result_class
**`pipeline_completed`**（本步只做 PDF/LaTeX 文本与图提取，无仿真、无数值物理验证；按硬规则上限即 pipeline_completed）。

## provenance（五字段）
- **source_artifact**：Akimov, "Mie scattering theory: A review of physical features and limitations", arXiv 2401.04146 (2024)；LaTeX 源 `Text-rev.tex` + PDF `2401.04146.pdf`；case `0703-01-akimov-mie-v1`
- **evidence_type**：文本/公式/图 caption 逐字提取（来自 LaTeX 源）+ 渲染图人工读轴 + Grep 核实无表格
- **timestamp_version**：20260703-2236
- **scope_applicability**：仅本步 01-pdf_preprocessing 的结构化提取产物；不含任何物理复现声明；图坐标轴为人工读数（非高精度数字化）
- **confidence_result_class**：高置信 / `pipeline_completed`

## retry_fingerprint
本步一次成功，无重跑。`step=01;round=0;changed=初次执行;new_evidence=LaTeX源+9页电子PDF成功提取,12图渲染;hypothesis=电子版无需OCR且LaTeX源可干净取公式;expected_signal=4个md+12png产出且公式可读` → 全部达成。
</content>
