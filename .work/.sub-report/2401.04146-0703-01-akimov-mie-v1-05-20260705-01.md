# step05 theory_check 工作报告

## 固定头 6 字段
- **role**: sub-agent（W-sub 复现执行者，探针执行）+ main-agent（编排层文档汇总，因 sub 文档阶段截断）
- **task_scope**: 执行 05-theory_check，对 scattering.py + akimov_coeffs.py 做对抗式审查（8 探针），只审查不改功能，不进 T3
- **evidence_refs**:
  - `.work/.todo/2401.04146/0703-01-akimov-mie-v1/05-theory_check/adversarial_probes.py`（探针脚本，sub 落盘）
  - `.work/.todo/.../05-theory_check/adversarial_probes.txt`（8 探针原始 stdout，sub 落盘）
  - `.work/.todo/.../05-theory_check/theory_check.md`（审查报告，main 汇总）
  - `.paper/scattering.pdf`（BH 教材，符号核对源）
  - reproduction_test/mie/code/{scattering.py, akimov_coeffs.py}（被审）
- **confidence**: 高。8 探针覆盖全审查维度，符号用教材解析式独立核（非 verifier 反推），T2 独立性 AST 证实。
- **blocked_by**: 无
- **recommended_action**: 审查通过无实质 bug，result_class 维持 simulation_completed。可进 step06（Fig3 求根 T3）。step06 从 ε 比算 m 用 np.sqrt 主值即可（P8 偶函数保证分支无关）。

## 8 字段主体

### 1. 身份声明
本步为 step05 theory_check（对抗式代码审查）。执行分两部分：探针脚本由 sub-agent 编写并运行落盘（adversarial_probes.py/.txt）；文档汇总（theory_check.md + 本报告 + memento JSON）因 sub 连续两步在文档阶段被 max_turns/截断打断，由 main-agent 依 sub 落盘的探针原始 stdout 汇总——所有数值来自 sub 的 stdout，未二次运行，未篡改。

### 2. 做了什么
写 8 个对抗性探针证伪代码：P1 Rayleigh 符号对教材、P2 负 ε 双 sqrt 分支、P3 角点溢出、P4 极点静默错值、P5 T2 AST 伪独立检测、P6 大 l 发散、P7 符号翻转注入对照、P8 m 偶函数对称。全部未推翻代码。

### 3. 用了什么
scipy.special（不自写特殊函数）；ast 模块（解析 akimov_coeffs 真实 import）；BH 教材式 4.56/4.57 Rayleigh 解析极限作独立符号判据；np.sqrt 双分支对照。

### 4. 遇到什么问题
- sub-agent 连续两步（step04、step05）在文档落盘阶段截断，探针/代码落盘但报告缺——已由 main 汇总补齐，并记为框架数据点（sub 单步产物过多易触 max_turns，建议 step10 记入 toEflow）。
- P4 探针触发 m=0j 的 RuntimeWarning（invalid divide）——非代码 bug，是探针故意测退化输入，代码 graceful 返回 nan。

### 5. 结果
无实质 bug，代码未改（git status 干净）。审查结论：BH 式逐项正确、$\xi_l=+iy$ 符号正确（对教材核）、负 ε 域数值可用且分支无关（偶函数）、T2 真独立、截面守恒解析恒等、极点/大 l graceful。

### 6. 决策性回答（6 问）
1. **BH 式逐项一致吗？** 一致。a_num/a_den/b_num/b_den 逐项对 spec/教材，a 与 b 的 m 因子位置正确互换（电磁对偶）。无错位。
2. **符号/时谐核对结论？verifier 能否反推？** $\xi_l=+iy_l$、$e^{-i\omega t}$ 正确（P1 Rayleigh 对教材 Im 同号 rel~1e-6；P7 注入对照逐位一致）。verifier PASS **不能**反推符号——T2 两式同错会同过，energy/rayleigh 不看相位符号。必须教材独立核。
3. **负 ε 分支正确吗？角点稳定性？** 正确且分支无关（P8 偶函数 max diff 1.76e-16）。角点 l=3,ε=−10,q_e=10 系数 O(1) finite，raw |ψ|=2.2e13≪float64 max。两 sqrt 分支均有限给同值。
4. **T2 独立还是伪独立？** 有效独立。P5 AST 证实 akimov_coeffs 无 import scattering、不调 mie_ab；三独立路径（BH/Akimov/ξ=ψ−iχ）一致。
5. **发现 bug 吗？** 无实质 bug，代码未改。证伪路径 8 条全未推翻（见 theory_check.md §8）。
6. **result_class？** `simulation_completed`（维持 step04 经 Gate3 的上游结论，审查加固；物理成功仍待 Layer3）。

### 7. 下一步输入
step06（Fig3 求根 T3）：用 scattering.mie_ab 在 (q_e, ε_i/ε_e) 平面求根 Im(a_l)=0 → 分类 a_l∈{0,1}（酉性实数化，见 formalization.yaml solver）。从 ε 比算 m 用 np.sqrt 主值（P8 保证分支无关）。TM=a_l、TE=b_l，l=1,2,3，共 6 面板。

### 8. 长期记忆更新
落 `.work/memento-cache/2401.04146-step05-20260705-01.json`（sub 无 MCP，main 代回灌）。关键条目：符号独立核方法（Rayleigh 教材式）、m 偶函数（负 ε 分支无关）、T2 AST 独立性判定法。

## uncertainty / missing_evidence
- uncertainty：探针数值由 main 从 sub stdout 转录，未二次运行——若需 100% 无中介可由用户复跑 adversarial_probes.py。
- missing_evidence：无。8 维度均有探针证据。
- result_class：`simulation_completed`

## provenance
- source_artifact: adversarial_probes.{py,txt}（step05, 20260705-01）+ scattering.py/akimov_coeffs.py + BH 教材式 4.56/4.57
- evidence_type: 8 对抗探针原始 stdout + AST 解析 + 教材解析极限独立核对
- timestamp_version: 20260705-01
- scope_applicability: 单球 Lorenz-Mie 核，l=1..3, ε∈[-10,15], q_e∈(0,10]，Fig3 域
- confidence_result_class: 高 / simulation_completed
