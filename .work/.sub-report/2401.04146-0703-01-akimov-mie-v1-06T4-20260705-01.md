# 子 agent 工作报告 — step06 T4（出图 + 数字化 + 完备性核对 + step08 接口）

## 固定头 6 字段

- **role**: 子 agent（W-sub，复现执行者），被 main-agent spawn 做 step06 T4 收尾子任务
- **task_scope**: 用 T3 六 CSV 画 Fig3 六面板复现图 + 二维 contour 完备性核对(防漏根,Gate2 强制) + 数字化原 Fig3 + 叠图 + 为 step08 量化对比准备接口。不算 step08 最终 Layer3 指标（只准备）。
- **evidence_refs**:
  - `reproduction_test/mie/figures/fig3_repro.png`（六面板复现图）
  - `reproduction_test/mie/figures/fig3_contour_check.png` + `.work/.todo/2401.04146/0703-01-akimov-mie-v1/06-run_and_monitor/completeness_check.txt`（完备性核对）
  - `reproduction_test/mie/data/fig3_digitized.csv`（数字化 1982 点）
  - `reproduction_test/mie/figures/fig3_overlay.png`（叠图）
  - `.work/.todo/2401.04146/0703-01-akimov-mie-v1/06-run_and_monitor/step08_compare_interface.md`（step08 接口）
  - 代码：`code/fig3_plot.py`、`code/fig3_contour_check.py`、`code/fig3_digitize.py`（新增，不改 scattering.py/fig3_loci.py）
- **confidence**: 高（六面板形貌与原图一致 + 完备性逐面板支数一致 PASS + 叠图目测吻合）；数字化本身带主观误差（中）
- **blocked_by**: none
- **recommended_action**: 放行进 step08 跑 Layer3 定量（按 step08_compare_interface.md）。完备性 Gate2 判据已 PASS，可直接引用。

---

## 1. 身份声明

我是子 agent（W-sub），不是 main-agent。不决定 workflow 走向、不碰 `.result/`、不改 `.claude/skills/`、不宣布物理复现成功。本步最高 result_class = `simulation_completed`（Layer3 量化在 step08 + Gate4）。

## 2. 做了什么

分 5 批落盘（防截断），批次 1-2 已经 main 独立核过放行：

1. **六面板复现图**：`fig3_plot.py` 读六 CSV，按 `(type,branch_id)` 分组、相邻 eps 跳变 >3 断线（不跨渐近支连线），红虚 sr / 蓝实 nr，轴 [0,10]×[-10,15]。→ `fig3_repro.png`。
2. **二维 contour 完备性核对**：`fig3_contour_check.py` 复用 `fig3_loci.coeff_vec`（不改该文件），500×900 网格算 Im(coeff)，`contour(level=0)` 提零线按 Re 分类，与切片法 CSV 叠加 + 逐面板支数计数 + 覆盖统计（contour 点到最近切片点归一化距离）。→ `fig3_contour_check.png` + `completeness_check.txt`。
3. **数字化**：`fig3_digitize.py` 颜色像素法。矢量源 `Fig3-eps-converted-to.pdf` @4x 渲染（2288×3256），red/blue mask 分离 sr/nr，六面板 dark-pixel 密度探测帧边界后仿射标定，q_e 分箱(40) + eps 聚类抽代表点。→ `fig3_digitized.csv`（1982 点）。
4. **叠图**：`fig3_plot.py overlay`，复现曲线（线）叠数字化点（散点）。→ `fig3_overlay.png`。
5. **step08 接口 + 报告 + memento**：`step08_compare_interface.md` + 本报告 + memento-cache JSON。

## 3. 用了什么

- Python: numpy 2.3.5, scipy 1.16.3, matplotlib 3.10.6, PIL, PyMuPDF(fitz)。
- 复用（只调用不改）：`fig3_loci.py` 的 `coeff_vec`（向量化 BH 核，与审计过的 scattering.mie_ab 逐字一致）。
- 数字化用颜色阈值法（非手工点选），比 WebPlotDigitizer 手工更客观、可复现。
- 未 spawn sub-leaf：数字化用颜色法脚本一次跑通，自己做更快，无需外包。

## 4. 遇到什么问题

- **中文字体缺失**：matplotlib DejaVu Sans 缺中文字形（"支""段"），标题改英文规避；不影响图内容。completeness_check.txt 走 `contextlib.redirect_stdout` 捕获后以 UTF-8 写盘（中文正常）。
- **tee 相对路径失败**：改用 Python 直接写绝对路径 UTF-8 文件。
- **junction 路径**：`reproduction_test/mie` 是到 optics_agent 的 junction，产物物理落在 optics_agent 侧，逻辑路径不变，符合目录约定。
- 无 blocker。

## 5. 结果（产物路径 + 关键数值 + 验证状态）

**产物（全部落盘）**：
| 产物 | 路径 | 状态 |
|------|------|------|
| 六面板复现图 | `reproduction_test/mie/figures/fig3_repro.png` | ✅ |
| 完备性 contour 图 | `reproduction_test/mie/figures/fig3_contour_check.png` | ✅ |
| 完备性核对 stdout | `.work/.todo/.../06-run_and_monitor/completeness_check.txt` | ✅ |
| 数字化 CSV | `reproduction_test/mie/data/fig3_digitized.csv` | ✅ 1982 点 |
| 叠图 | `reproduction_test/mie/figures/fig3_overlay.png` | ✅ |
| step08 接口 | `.work/.todo/.../06-run_and_monitor/step08_compare_interface.md` | ✅ |
| memento JSON | `.work/memento-cache/2401.04146-step06T4-20260705-01.json` | ✅ |

**关键数值**：
- 复现图支数（sr/nr branch，含平凡线 bid=0）：TM l=1 12/12、l=2 12/12、l=3 11/11；TE l=1 12/12、l=2 11/11、l=3 11/11。
- **完备性核对（Gate2 强制判据）**：切片法 branch 数 == contour 段数 **逐面板完全一致**（TM 12/12,12/12,11/11；TE 12/12,11/11,11/11）；contour 点被切片法覆盖，未覆盖率全 <0.2%（最大 TE l=1 sr 0.18%）。中位归一化覆盖距离 0.0005。**无系统性漏根/杂散支，PASS**。
- 数字化：每面板每类型 143-179 点（远超 ≥15），共 1982 点；不确定度估 dq_e~0.05、dε~0.15（归一化 ~0.005-0.006）。

**验证状态**：Layer1（能量守恒/瑞利/大尺寸，T1）与 Layer2（sr/nr 根自洽，T3）此前已过；本步补 Gate2 完备性判据 PASS。Layer3 量化未做（step08）。

## 6. 决策性回答 ★（逐条回答 main 的 6 决策问题）

1. **六面板复现图画出来了吗？各面板 sr/nr 支数？与原 Fig3 目测整体形貌一致吗？**
   画出来了。支数见上（TM 12/12,12/12,11/11；TE 12/12,11/11,11/11）。**整体形貌与原 Fig3 一致**：ε=1 平凡 nr 水平线在；TM 负 ε 红虚低谷 l=1≈-5@q_e1.2 / l=2≈-5.3@q_e2 / l=3≈-6@q_e3，随 l 增大右移下移（与 step03 锚点 eps=-4.640@q_e≈1 一致）；分支扇形从纵轴/eps=1 上方向右下发散的走向、密集程度均吻合。

2. **完备性核对：切片法 vs contour 法逐面板支数一致吗？有无漏支/杂散支？（Gate2 强制，逐面板列）**
   **逐面板完全一致，无漏支无杂散支**：
   | 面板 | 切片 sr/nr | contour sr/nr | 覆盖未达标率(sr/nr) |
   |------|-----------|--------------|-------------------|
   | TM l=1 | 12/12 | 12/12 | 0.09%/0.09% |
   | TM l=2 | 12/12 | 12/12 | 0.11%/0.00% |
   | TM l=3 | 11/11 | 11/11 | 0.10%/0.00% |
   | TE l=1 | 12/12 | 12/12 | 0.18%/0.00% |
   | TE l=2 | 11/11 | 11/11 | 0.15%/0.17% |
   | TE l=3 | 11/11 | 11/11 | 0.17%/0.00% |
   contour 独立法找到的 loci 全部被切片法 CSV 覆盖（未覆盖率 <0.2%，残余是网格边界/退化点邻域像素级差异），**Gate2 "曲线支数逐面板一致" PASS**。

3. **数字化：用矢量还是位图源？每条曲线取样几点？方法和不确定度？**
   用**矢量源** `Fig3-eps-converted-to.pdf`（@4x 渲染，比位图 png 精确）。方法：**颜色像素提取**（red mask=sr虚线、blue mask=nr实线），六面板 dark-pixel 密度探测帧边界后仿射标定，q_e 分箱(40)+eps 聚类抽代表点——非手工点选，客观可复现。每面板每类型 **143-179 点**（远超 ≥15 要求；分箱粒度可调）。**不确定度**：像素→数据标定 ~1px；面板 x 跨 ~595px/q_e10=0.017/px、y 跨 ~508px/eps25=0.049/px，叠加线宽(~3px)+帧定位(~2px)，估 dq_e~0.05、dε~0.15，归一化后 ~0.005-0.006（与 Layer3 中位阈值 0.01 同量级）。**如实声明：这是数字化主观误差，不过度声称精度。**

4. **叠图目测吻合度如何？有无明显偏离的面板/曲线？**
   **目测吻合良好**：数字化 sr 点(暗红圆)贴红虚复现曲线、nr 点(navy方)贴蓝实复现曲线；ε=1 平凡线、负 ε sr 低谷、分支扇形全对齐。**无系统性偏离的面板/曲线**。仅密集渐近区（右上 q_e→10、ε→大）有像素级小偏移，属数字化不确定度（曲线挤在一起时颜色 mask 与分箱聚类易串邻支），非复现曲线错误。**未触发回修**。注意：目测吻合 ≠ Layer3 通过，量化在 step08。

5. **step08 接口准备好了吗？指标/阈值/路径/签名齐吗？**
   齐了。`step08_compare_interface.md` 含：指标定义（数字化点到同面板同类型复现曲线族最近归一化距离，q_e/10、ε/25）、建议阈值（中位<0.01、95分位<0.03、支数逐面板一致[已PASS]，**标注 SEPR 自定非社区标准**）、数据路径（六 loci CSV + digitized CSV + completeness + overlay）、脚本签名建议（`load_repro_curve`/`load_digitized`/`nearest_norm_dist`/`panel_metrics`/`layer3_verdict`，建议 cKDTree 加速）。step08 可直接实现。**本步不算最终指标**。

6. **本步 result_class？**
   `simulation_completed`。理由：仿真数据（复现图+完备性）与数字化数据备齐、Gate2 完备性判据 PASS，但 Layer3 量化对比未做（只准备接口）、无物理成功声明。**不是** partial_physical_match（Layer3 未量化）、**不是** physical_reproduction_success（需 Gate4）。

## 7. 下一步需要的输入（接力信息）

- step08 执行方：按 `step08_compare_interface.md` 实现 `fig3_compare.py`，算逐面板 sr/nr 的中位/95分位归一化距离，出 Layer3 verdict。完备性 Gate2 判据已 PASS，直接引用 `completeness_check.txt`，不必重算。
- Gate2/Gate4 用户裁决点：Layer3 阈值（中位<0.01、95分位<0.03）SEPR 自定无社区先例，须人审认可；step08 报告须显式标注。
- 若 step08 量化在密集渐近区超阈值，先查是否数字化误差（该区不确定度最大）而非复现错误——可加密数字化分箱或用切片法密采样点直接比对。

## 8. 长期记忆更新

**MCP 预检**：本 session 执行层无 MCP（正常设计，非故障），memento 不可用 → 记忆纪律降级，关键结果已落 `.work/memento-cache/2401.04146-step06T4-20260705-01.json`（5 条记忆，均带 provenance 五字段），供 main 回灌 memento。未假装搜/存 memento。

**关键可复用经验**（已入 JSON）：
- loci 型复现的完备性核对（防漏根）用"二维 contour 独立法 vs 切片法逐面板支数 + 覆盖"，是 Gate2 强制附加判据的标准做法。
- 彩色矢量 loci 图数字化用颜色阈值 mask 法（红/蓝分明时）比手工点选客观可复现；帧边界用 dark-pixel 密度探测自动定位。
- 目测吻合 ≠ Layer3 通过，务必区分（本步只到 simulation_completed）。

---

### uncertainty / missing_evidence

- **uncertainty**：数字化主观误差（dq_e~0.05、dε~0.15，归一化~0.005-0.006）是 Layer3 对比主要不确定度来源；密集渐近区颜色 mask 可能串邻支，该区数字化点可信度低于低 q_e 区。完备性覆盖残余 <0.2% 是网格边界/退化点邻域像素级差异，非物理漏根。
- **missing_evidence**：Layer3 最终量化指标（step08 未跑）；Layer3 阈值的社区先例（loci 图无先例，SEPR 自定待 Gate4 认可）。除此 none。

**retry_fingerprint**：本步一次跑通，无重跑（retry_budget=5 未动用）。仅 2 处技术性修正（tee 相对路径→绝对路径 UTF-8 写盘；中文字体→英文标题），非物理重跑。
