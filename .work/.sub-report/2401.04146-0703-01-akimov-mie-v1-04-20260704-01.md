# step04 工作报告 — theory_and_implementation（T1+T2）

## 固定头 6 字段

- **role**: sub-agent（W-sub，复现执行者），被 main-agent spawn 做第 04 步 T1+T2。不决定 workflow 走向，不写 `.result/`，不改 `.claude/skills/`。
- **task_scope**: T1 = BH 主源 Lorenz-Mie 核 `scattering.py` + 跑三 Layer1 verifier；T2 = Akimov 式独立实现 `akimov_coeffs.py` + 逐点交叉验证 `crosscheck_bh_vs_akimov.py`。**不进 T3**。
- **evidence_refs**:
  - `reproduction_test/mie/code/scattering.py`（T1 BH 核）
  - `reproduction_test/mie/code/akimov_coeffs.py`（T2 Akimov 独立实现）
  - `reproduction_test/mie/code/crosscheck_bh_vs_akimov.py`（T2 比对脚本）
  - `.work/.todo/2401.04146/0703-01-akimov-mie-v1/04-theory_and_implementation/verifier_log.txt`（5 项原始 stdout）
  - `.work/.todo/2401.04146/0703-01-akimov-mie-v1/04-theory_and_implementation/derivation.md`（推导+核对+诊断）
  - `.work/memento-cache/2401.04146-step04-20260704-01.json`（降级记忆）
- **confidence**: 高（数值证据充分：能量守恒严格 0、T2 max 差 2.2e-15、独立算法交叉验证一致）。
- **blocked_by**: 大尺寸 verifier `check_large_size_limit.py` FAIL（max$|Q_{ext}-2|=0.1711$ @ $x=50$，tol 0.05），诊断为**疑似 verifier 阈值/采样点不当（非实现错）**，待 main/human 在 Gate3 裁决（放宽 tol / 改测试 x / 换阻尼介质 / 接受）。
- **recommended_action**: 补齐产物后交 main-agent，停 Gate3，请用户同时裁决 (a) BH $a_l/b_l$ 公式对教材核对 (b) 大尺寸 verifier FAIL 处理方式。

---

## memory_search_summary（MCP 降级说明）

执行层 sub-agent 的 tools allowlist 结构性不含 MCP（GATE2-决定.md 第 7 发现：刻意设计，非 bug；执行层不独立写长期记忆，避免 case 级半成品污染全局库）。因此**无法调 memento**，未搜索/未写入 memento 工具。降级方式：关键事实/教训写成结构化 JSON 落 `.work/memento-cache/2401.04146-step04-20260704-01.json`（provenance 五字段），由 main-agent 审后代回灌。

从输入采用的设计决策：
- `formalization.yaml` `equations.primary_BH` → BH $a_l,b_l$ 主源公式（本步实现依据）；`cross_check_akimov` → T2 独立式。
- `lossless_unitarity`（酉性实数化）→ 能量守恒/无耗吸收=0 的物理判据。
- repro_plan T1 接口契约 + GATE2 决定 3（T2 blocker 口径、正式脚本固化不得拿 step03 预验证顶）→ crosscheck 本步重新落盘。

---

## 1. 身份声明

我是子 agent（sub-agent / W-sub），执行第 04 步 T1+T2。上一轮被截断，本轮补齐 4 项硬交付（verifier_log.txt / derivation.md / 报告 / memento JSON），代码不改。所有 verifier 与诊断脚本本轮**自己重跑**落盘，未抄 main-agent 转述的数字当日志。

## 2. 做了什么

1. 读 `formalization.yaml`（物理 spec）、`repro_plan.md`、`GATE2-决定.md`、三 verifier 源码（接口契约）。
2. 环境核对：numpy 2.3.5 / scipy 1.16.3，复宗量 `spherical_jn/yn` 可用；中心差分核对 $\psi_l'$ 导数约定（差 1.4e-10）。
3. 写 `scattering.py`（BH 核，Riccati-Bessel + $a_l,b_l$ + 光学定理截面 + Wiscombe 截断）。
4. 跑三 Layer1 verifier + 自测：能量守恒 PASS（0）、Rayleigh PASS（4.0001）、大尺寸 **FAIL**（0.1711）。
5. 大尺寸 FAIL 根因诊断（三独立证据）：截断充分性表、独立 Wiscombe 递推交叉验证、x-sweep 趋 2 表。
6. 写 `akimov_coeffs.py`（独立实现，自有 Riccati-Bessel 路径，不 import scattering.mie_ab）+ `crosscheck_bh_vs_akimov.py`（3000 确定性 + 300 随机点，极点跳过机制）。
7. 跑 T2：max$|\Delta a|=2.221$e-15、max$|\Delta b|=4.775$e-16，PASS，0 极点跳过。
8. 全部原始 stdout `tee` 落 `verifier_log.txt`；写 `derivation.md`、memento JSON、本报告。

## 3. 用了什么

- 工具：Read / Write / Bash（`PYTHONUTF8=1 python`）。**未 spawn sub-leaf**（本步规模自扛）。
- 库：`scipy.special.spherical_jn / spherical_yn`（含 `derivative=True`，复宗量），numpy。不自写特殊函数、不自写递推（诊断用的独立 Wiscombe 递推仅为交叉验证，不进产物核）。
- 主源：BH 教材 §4.3/§4.4（经 `formalization.yaml` spec 转写）。

## 4. 遇到什么问题

**大尺寸消光佯谬 verifier FAIL**（唯一问题）。诊断（见 derivation.md 第五节，本轮独立复跑确认）：
- 截断充分：$x=50$ 处 $n_{max}$ 67→97→147，$Q_{ext}$ 恒 2.171073 不变（排除漏项）。
- 独立算法一致：Wiscombe 下行递推与 scipy 路径 $x=50$ 均 $Q_{ext}=2.17107$（排除 scipy bug）。
- 物理本质：$Q_{ext}\to2$ 是 $x\to\infty$ 渐近极限，无损 $m=1.5$ 收敛慢 + Mie ripple（$x=120$ dev 0.045 < $x=200$ dev 0.092，非单调 = 振荡），要 $x\gtrsim1000$ 才稳 <0.05。verifier 采样 $\{50,80,120,200\}$ 恰落 ripple 未衰减区。
- 结论：**非 scattering.py 实现错**，疑似 verifier 阈值/采样点设计不当。我**无权改 verifier**（human gate），仅诊断如实报告。

retry：未触发（无重跑；FAIL 一次即完成诊断，无新假设可改代码，属 verifier 侧问题）。

## 5. 结果

| 产物 | 路径 | 状态 |
|------|------|------|
| BH 核 | `reproduction_test/mie/code/scattering.py` | 已落盘，接口契约匹配 |
| Akimov 独立实现 | `reproduction_test/mie/code/akimov_coeffs.py` | 已落盘 |
| 交叉验证脚本 | `reproduction_test/mie/code/crosscheck_bh_vs_akimov.py` | 已落盘，PASS |
| 推导+诊断笔记 | `.../04-theory_and_implementation/derivation.md` | 已落盘 |
| 原始 stdout | `.../04-theory_and_implementation/verifier_log.txt` | 已落盘（5 项） |
| 降级记忆 | `.work/memento-cache/2401.04146-step04-20260704-01.json` | 已落盘 |

关键数值：能量守恒 max rel err **0.000e0** PASS；Rayleigh 斜率 **4.0001** PASS；大尺寸 max$|Q_{ext}-2|=$**0.1711** @ x=50 **FAIL**；T2 max$|\Delta a|=$**2.221e-15**、max$|\Delta b|=$**4.775e-16** PASS（0 极点跳过）；实 m 无耗 $|C_{abs}|/C_{ext}=$**1.29e-16**。

**验证状态**：Layer1 三取二 PASS，大尺寸 FAIL（诊断为 verifier 侧）；Layer2-C（T2 交叉一致）PASS。

## 6. 决策性回答（spawn 指令 6 问，贴实测数字）

1. **T1 三 verifier 全 PASS 了吗？实测值？** 否，二 PASS 一 FAIL。能量守恒 max rel err **0.000e0**（PASS，tol 1e-10）；Rayleigh 拟合斜率 **4.0001**（PASS，期望 4±0.01）；大尺寸 max$|Q_{ext}-2|=$**0.1711** @ x=50（**FAIL**，tol 0.05，诊断为 verifier 侧非实现错）。

2. **T2 max$|\Delta a|$、max$|\Delta b|$？<1e-12 吗？跳过多少极点？** max$|\Delta a|=$**2.221e-15**、max$|\Delta b|=$**4.775e-16**，均 ≪ 1e-12，**PASS**。**0** 个极点跳过（`|den|<1e-9` 阈值下 3300 点均未命中 loci 分母零点）。

3. **复 m 路径稳定吗？角点量级？** 稳定，3300 点无 NaN/溢出/极点跳过。scipy 复宗量球 Bessel 直接可用。角点 $l=3,\varepsilon=-10,q_e=10$ 处 $\psi_l(q_i)$ modified-Bessel 型指数增长，$|$分子$|\sim5\times10^{14}$，float64 安全（step03 已核，本步一致），无需重标度。

4. **本步 result_class 上限？** **`diagnostic_only`**。机械规则：Layer1 任一 FAIL（大尺寸）→ $\le$ `diagnostic_only`，不论根因。不向上包装为 `simulation_completed`。

5. **接口签名与 verifier 完全匹配了吗？** 是，逐个核对：`compute_cross_sections(m,x)->(Cext,Csca,Cabs)`、`compute_Q_sca(m,x)`、`compute_Q_ext(m,x)`、`mie_ab(l,m,x)->(a_l,b_l)`，`CODE_DIR=reproduction_test/mie/code`。三 verifier 均无 ImportError（若不匹配会返回 rc=2）。未改任何 verifier 迁就实现。

6. **Gate3 需特别核对的公式细节？** (a) 导数约定：$'$ 对宗量求导，$\psi_l'(z)=j_l(z)+z\,j_l'(z)$，用 scipy `derivative=True`（中心差分已核，差 1.4e-10）；(b) $\xi_l$ 符号约定：$e^{-i\omega t}$ 下 $\xi_l=z h_l^{(1)}=z(j_l+i y_l)=\psi_l-i\chi_l$，$\chi_l=-z y_l$，散射场用 $h^{(1)}$ 外向辐射，BH/Akimov 两源一致无需翻转；(c) $C=Q$（$\pi R^2$ 无量纲化约掉），三截面共用同套 $a_l,b_l$ 保证能量守恒解析恒等——请对 BH (4.53)/(4.54) 核对。

## 7. 下一步需要的输入

- **Gate3 停机**：用户裁决 (a) BH 公式对教材核对 (b) 大尺寸 verifier FAIL 处理方式（放宽 tol / 改 x / 换阻尼介质 / 接受）。
- Gate3 放行后进 T3（Fig3 求根/loci），接力信息：`scattering.mie_ab(l,m,x)` 已就绪供 T3 直接取单系数；酉性实数化路线（$\mathrm{Im}\,a_l=0\Leftrightarrow a_l\in\{0,1\}$）已在 spec `solver`，无需重推。
- **uncertainty**："verifier 设计不当"最终定性属 human gate 权限，非本 agent 定论。
- **missing_evidence**：无第三方参考库（miepython 本机未装）做绝对基准；已用独立 Wiscombe 递推 + 解析 Rayleigh 振幅 + 能量守恒严格 0 三重内证替代，足以支撑"系数正确"。

## 8. 长期记忆更新

执行层无 MCP，降级落 `.work/memento-cache/2401.04146-step04-20260704-01.json`（含 2 facts + 2 pitfalls + 1 decision，均带 provenance 五字段），由 main-agent 审后代回灌。核心可复用条目：
- BH 核实现正确（能量守恒严格 0 + 独立算法交叉验证）。
- T2 BH/Akimov 两式机器精度等价（等价来源：链式法则 k 因子 BH 吸收进 m、Akimov 显式保留）。
- **踩坑**：无损介质球大尺寸 $Q_{ext}\to2$ verifier 采样点须避 ripple 区或用阻尼介质，否则 FAIL 非实现错。
- 复 m（负 eps 纯虚 m）scipy 路径稳定，float64 安全无需重标度。
