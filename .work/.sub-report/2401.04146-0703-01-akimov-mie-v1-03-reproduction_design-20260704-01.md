# 子 agent 工作报告 — 03-reproduction_design

```yaml
role: sub-agent（W-sub，复现执行者）
task_scope: 执行 03-reproduction_design：论文 prose → 结构化物理 spec（formalization.yaml）+ 复现拆分计划（repro_plan.md），目标图 = Fig3（Gate1 用户裁决）。不决定 workflow 走向。
evidence_refs:
  - .work/.todo/2401.04146/0703-01-akimov-mie-v1/formalization.yaml（本步产物1）
  - .work/.todo/2401.04146/0703-01-akimov-mie-v1/repro_plan.md（本步产物2）
  - .work/.todo/2401.04146/0703-01-akimov-mie-v1/GATE1-决定.md（输入约束）
  - .work/.todo/2401.04146/0703-01-akimov-mie-v1/{formulas.md, parameter_table.md, figures.md, missing_info.md, paper_understanding.md, figs/Fig3.png}（step01/02 输入）
  - .claude/skills/optics-mie-reproduction/scripts/check_energy_conservation.py（verifier 接口契约实读）
  - step03 内联数值预验证（本报告"结果"节记录输出数值）
confidence: 高（spec 9 字段闭合 + 关键结构性质经数值预验证）
blocked_by: none
recommended_action: main-agent 停机触发 Gate2（spec 核对），把 formalization.yaml + repro_plan.md 呈现用户；重点请用户认可 (1) Fig3 范围边界与"显式不做"清单 (2) Layer3 建议阈值（归一化距离中位<0.01，无文献先例）(3) T1→{T2,T3}→T4 拆分。Gate2 通过后 spawn step04（T1 BH 核实现）。
```

---

## 1. 身份声明

我是 sub-agent（W-sub，复现执行者），被 main-agent spawn 执行第 03 步 `reproduction_design`。paper=2401.04146，case=0703-01-akimov-mie-v1，timestamp=20260704-01。本步为设计步，无仿真、无物理验证声明。

## 2. 做了什么

1. 跑 `skill-print.py` 确认技能列表；读身份 skill（spawn 注入）+ 本步 workflow skill `.claude/skills/sub-agent/workflow/03-reproduction_design/SKILL.md`。
2. MCP 预检：**memento 不可用**（详见第 4 节），降级落 `.work/memento-cache/`。
3. 读全部授权输入：GATE1-决定.md、formulas.md、parameter_table.md、figures.md、missing_info.md、paper_understanding.md、figs/Fig3.png（渲染图目测核对轴范围与线型约定）。
4. 实读 verifier 脚本 `check_energy_conservation.py` 确认接口契约（`compute_cross_sections(m, x)`、`CODE_DIR=reproduction_test/mie/code`、TOL 1e-10）。
5. **数值预验证**（一次性内联 scipy 试算，为 spec 的 solver 设计提供证据，非正式 T1/T2 交付）：
   - BH 式 vs Akimov 式 300 随机点等价性；
   - 无耗酉性 $|a_l|^2=\mathrm{Re}\,a_l$ 结构验证；
   - "分子=0 是实方程"捷径的证伪；
   - 负 $\varepsilon$ 极端角点数值幅度检查；
   - $l=1$ TM、$q_e=1$ 切片 brentq 求根试算并与 Fig3 目测对照。
6. 写 `formalization.yaml`（9 字段全闭合）+ `repro_plan.md`（T1–T4 拆分 + 三层验证映射 + 量化判据）。
7. 写本报告 + memento 降级缓存。

## 3. 用了什么

- 输入 artifact：step01/02 全部产物 + GATE1-决定.md + Fig3 渲染图（`.paper/` 未再读原 PDF，step01 的 LaTeX 源提取已覆盖所需公式且更可靠）。
- 工具：Read/Glob/Bash（python + scipy 试算）/Write。
- 预制脚本：本步 SKILL.md 提到 `formalization_template.py`，但 `scripts/` 目录不存在该文件（实查只有 SKILL.md），改为按 SKILL.md 字段清单手写 YAML——差异已如实记录，建议进 toEflow 修 SKILL 描述。
- 未 spawn sub-leaf：本步无需（数字化 Fig3 留给 T4/step08，届时再 spawn）。

## 4. 遇到什么问题

1. **memento MCP 不可用（本 session）**：spawn 指令称"MCP 已恢复"，但本子 agent session 中 `ToolSearch` 调用报 `No such tool available`，memory_* 工具未注入。按 CLAUDE.md 记忆纪律第 0 步降级：不假装搜过/存过，声明降级，结构化 JSON 落 `.work/memento-cache/20260704-01-step03-reproduction_design.json`（含 3 条待回灌记忆，provenance 五字段齐全），供 MCP 恢复后回灌。**非阻塞**（本步输入已由父指令 + step01/02 产物覆盖）。
2. 本步 SKILL.md 引用的预制脚本 `formalization_template.py` 实际不存在（见第 3 节），小偏差，绕过。
3. 设计中发现一个物理陷阱并当场消解：最初设想"$a_l=0\Leftrightarrow$ 分子=0，实参数下分子恒实"——数值证伪（负 $\varepsilon$ 域分子相位不定）。改用无耗酉性路线（见下），已写进 spec 并存为降级记忆。

## 5. 结果

**产物（全部已落盘）**：
- `.work/.todo/2401.04146/0703-01-akimov-mie-v1/formalization.yaml` — 9 字段（geometry/materials/equations/boundary_conditions/sources/solver/observables/assumptions/missing_fields）全闭合，missing_fields 4 条如实列出。
- `.work/.todo/2401.04146/0703-01-akimov-mie-v1/repro_plan.md` — T1→{T2,T3}→T4 拆分、量化判据、Layer1/2/3 映射、风险表。
- 本报告 + memento 降级缓存 JSON。

**关键数值（step03 预验证，inline scipy）**：
- BH vs Akimov 等价性：300 随机点（$l=1..3$，$\varepsilon\in[-10,15]$，$q_e\in(0,10]$）$\max|\Delta a_l|=4.7\times10^{-16}$，$\max|\Delta b_l|=3.5\times10^{-16}$ → 记号映射 $x=q_e$、$mx=q_i$、$m=\sqrt{\varepsilon_i/\varepsilon_e}$ 推导闭合，无 blocker。
- 无耗酉性：$\max||a_l|^2-\mathrm{Re}\,a_l|=4.4\times10^{-16}$ → $\mathrm{Im}\,a_l=0\Leftrightarrow a_l\in\{0,1\}$ 严格成立，两族 loci 统一为单实方程。
- 证伪记录：$\mathrm{Im}(N/i^l)$ 相对量级达 1.0 → "分子恒实"捷径不成立，不可用作求根条件。
- 切片试算：$l=1$ TM、$q_e=1$，$\varepsilon\in[-10,15]$ 内唯一 $\mathrm{Im}\,a_1=0$ 根在 $\varepsilon\approx-4.640$（残差 $5\times10^{-16}$，判为超辐射 $a_1=1$），与 Fig3 面板 1 红虚线低谷（目测 $\approx-5$ @ $q_e\approx1.2$）位置一致。
- 数值安全：极端角点 $l=3,\varepsilon=-10,q_e=10$ 处 $|N|\sim5\times10^{14}$，float64 安全。

**验证状态**：本步无正式 verifier 运行（T1 代码未写，Layer1 脚本会 FAIL-2 缺实现，属预期）；上述数值是设计预验证，正式验证由 T1/T2/T3 落盘脚本承担。

## 6. 决策性回答 ★（供 main-agent 拍板）

1. **整篇还是单图？** 单图 **Fig3** + Lorenz-Mie 核基础设施（Gate1 已裁决，本步执行不改）。范围边界：Fig4/5/7/8（需材料色散，不下载）、Fig6（复根）、Fig1/2（场分布）、§4 修正（Fig9–12）全部显式不在本次范围——已写入 repro_plan 第一节。
2. **拆几个子任务？** 4 个：T1 BH 主源 Mie 核（verifier 接口契约 `compute_cross_sections/compute_Q_sca/compute_Q_ext(m,x)` 已按脚本源码核实）→ T2 Akimov 式交叉验证（**失败即 blocker**，建议先于 T3）→ T3 Fig3 求根/loci → T4 出图+数字化对比准备。T2/T3 只依赖 T1，T4 依赖 T3。
3. **检验标准？** 全量化：Layer1 三脚本 PASS（能量守恒 rel<1e-10 等内置阈值）；Layer2 = T2 两式差 <1e-12 + T3 根残差 <1e-8 + 超辐射截面恒等式 $\sigma_{sca,l}/(\pi R^2)=2(2l+1)/q_e^2$ rel<1e-8 + $\varepsilon=1$ 平凡线抽查；Layer3 = Fig3 数字化点到复现曲线族归一化距离中位 <0.01、95 分位 <0.03（**建议值，无文献先例，请 Gate2 用户认可**）。
4. **需不需要数值计算脚本？** 需要。"解析求根"准确含义：$a_l$ 有闭式（不解 PDE、无 FEM），但 $a_l=1/0$ 的 loci 是超越方程，须数值求根。方案：固定 $q_e$ 切片 + 酉性实数化（$\mathrm{Im}\,a_l=0$ 单实方程）+ brentq + $\mathrm{Re}$ 分类；备选二维 contour 做完备性核对。理由与证据见 spec solver 节。
5. **需不需要 Magnus 云计算？** 不需要。标量特殊函数 + 一维求根，$800\times4000$ 网格 × 6 面板本地单核秒~分钟级。零 COMSOL/HPC/材料数据下载。

**本步是否具备继续下一步的最低证据？** 是——spec 9 字段闭合、接口契约实核、记号映射数值闭合。但**下一步前必须过 Gate2**（用户核对 spec），本 agent 不触发 gate、不模拟同意。

## 7. 下一步需要的输入（接力信息）

- **给 main-agent**：呈现 Gate2 的三个核对点——① Fig3 范围边界（"显式不做"清单）；② Layer3 建议阈值（中位 0.01/95 分位 0.03，需用户认可或另定）；③ T1–T4 拆分与"T2 失败即 blocker"口径。
- **给 step04 执行者**：物理输入只读 `formalization.yaml`（equations.primary_BH 节）；代码落 `reproduction_test/mie/code/scattering.py`；接口签名必须匹配 `check_*.py`（`compute_cross_sections(m,x)->(Cext,Csca,Cabs)` 等）；另暴露 `mie_ab(l,m,x)` 供 T2/T3。
- **环境提示**：子 agent session 的 MCP memento 可能未注入（本 session 实测），spawn 下一步时建议父 agent 提示执行者自行预检并准备降级。
- 待回灌记忆：`.work/memento-cache/20260704-01-step03-reproduction_design.json`（3 条）。

## 8. 长期记忆更新

**memory_search_summary**：本 session memento MCP 不可用（ToolSearch 报 No such tool available），**未能真实执行 memory_search**，纪律降级并显式声明。已知记忆（父指令转述）：ID e786ffac "SEPR首跑Akimov: 目标图假设落空+papers.md污染+Gate1待决策"——其中"Gate1 待决策"已过时（Gate1 已放行 Fig3），"papers.md 污染"仍有效（本步严格以 step01/02 产物为准，未采信 papers.md 预写描述）。

**memory_store 状态**：memory_dedup_check / memory_store / decisions_log / pitfalls_log 均无法调用 → 3 条记忆（求根方案决策、两式等价性事实、子 agent MCP 注入陷阱）已带完整 provenance 落 `.work/memento-cache/20260704-01-step03-reproduction_design.json`，待 MCP 恢复后回灌。

---

## uncertainty 与 missing_evidence（关键判断逐条）

| 判断 | uncertainty | missing_evidence |
|------|-------------|------------------|
| BH 式与 Akimov 式等价 | 低（300 随机点 1e-16 量级） | 正式 T2 落盘脚本 + 确定性网格（本步为 inline 试算，未固化） |
| 酉性求根方案正确 | 低（结构恒等式机器精度成立 + 单切片与图目测一致） | 全平面曲线族与 Fig3 的系统对比（T3/T4/step08 交付） |
| Fig3 目标域内无数值溢出 | 低（角点核过） | 全网格扫描的实际溢出统计（T3 交付） |
| Layer3 阈值合理 | 中（无文献先例，凭数字化误差量级估计） | 用户 Gate2 认可；数字化实际误差测量（T4） |
| Fig3 面板布局=上排TM/下排TE | 低（渲染图面板内标签 "l=1,TM" 等直接可读） | none |
| memento 记忆无遗漏冲突 | 中（未能真实搜索记忆库） | MCP 恢复后补搜 `2401.04146`/`Fig3`/`loci` 确认无冲突记忆 |

## result_class

**`pipeline_completed`**——本步为设计步：spec 与计划产出齐备、流程跑完；inline 数值试算属设计预验证而非正式仿真交付，无任何物理复现声明。上限依规则不得高于 pipeline_completed。

## provenance

```yaml
provenance:
  source_artifact: "2401.04146 step01/02 产物（formulas.md/parameter_table.md/figures.md/missing_info.md/paper_understanding.md/figs/Fig3.png）+ GATE1-决定.md + check_energy_conservation.py 源码 + step03 inline scipy 预验证"
  evidence_type: "论文原文公式转写核对 + verifier 源码实读 + 数值验证（等价性/酉性/切片求根）+ 渲染图目测"
  timestamp_version: "20260704-01"
  scope_applicability: "仅本 case Fig3 复现设计；spec 与拆分对纯实 ε 比单球 Mie 有效，不适用于有耗/复 ε/多层场景"
  confidence_result_class: "高 / pipeline_completed"
```

## retry_fingerprint

`step=03;round=1;changed=首轮无重跑;new_evidence=n/a;hypothesis=n/a;expected_signal=n/a`（本步一次通过，未消耗 retry 预算）
