# 缺失信息清单 — Akimov 2401.04146（case 0707-02，聚焦 Fig6）

> 步骤：02-paper_reading（子 agent / W-sub）
> case：0707-02-akimov-mie-v1 ｜ timestamp：20260709
> 原则：缺失明确列，不假装有。本次因上级消息要求立即落盘，部分本应更精确核实的项目（坐标轴精确刻度、分支精确计数）改为在此列为"需人工核"，未做进一步图像精确核对。

---

## 1. 已确认缺失 / 需人工核（阻塞 step03/04 前需决策）

| # | 缺失项 | 影响 | 能否补 / 怎么补 | 阻塞级别 |
|---|--------|------|----------------|---------|
| 1 | **Fig6 坐标轴精确数值范围**未做像素级精确核对（本报告仅目视估读：Re 约 $[-5,15]$，Im 因面板而异约 $[0,1.2]$–$[0,4]$） | 若 step08 定量对比时坐标轴范围理解有误，会导致数字化点位置系统性偏移 | 能补：后续 step 需对 `figs/Fig6.png` 做真正的坐标轴刻度像素定位（仿照 Fig3 数字化脚本的坐标系标定环节） | 非阻塞（不影响 step03/04 的求根算法设计，只影响 step08 数字化精度） |
| 2 | **Fig6 各面板精确分支数**未逐支计数（本报告仅引用图上可见数字标签估计 TM/TE 约 10-12 支） | 影响"完备性核对"判据的精确阈值（该用几支做基准） | 能补：数值求根完成后，用求出的分支数与图上数字标签数量比对，不需要现在就精确计数——**建议把"精确计数"推迟到 step04 求根之后**，用代码结果反推，而非现在纯目视强行数清楚（本报告已如实标注为估计值） | 非阻塞 |
| 3 | **`scipy.special.spherical_jn`/`spherical_yn` 是否支持复数宗量输入** | 本 case 核心实现风险：$q_i=k_iR$ 因 $\varepsilon_i$ 复数而为复数，若 scipy 版本/实现不支持复数宗量，需要替代实现（如用半整数阶 `jv`/`yv` 换算） | 能补：step04 实现前先用简单脚本测试 `spherical_jn(1, 1+0.5j)` 是否报错或给出正确复数值；若不支持则改用 `scipy.special.jv(l+0.5, z)*sqrt(pi/(2z))` 等价式（trust：教材标准换算） | **潜在阻塞**（若不支持且替代实现有误，会导致 step04 从根源出错，建议 step04 开始前第一步就是验证这一点） |

## 2. 论文未明确给出但可从教材/惯例补全（非阻塞，沿用 Fig3 case 已确认的做法）

| # | 缺失项 | 能否补 | 来源 |
|---|--------|--------|------|
| 4 | 论文未给数值求根算法细节（用什么优化器、初值如何选取、如何保证找全所有分支） | 能补：paper_understanding.md 第3节已给出建议方案（fsolve 二维求根 + 多起点撒点 + 沿 $q_e$ 延拓），但这是**本 case 自己设计的方案，非论文原文给出**，需 step03/04 定夺是否采用或改进 | trust（本 case 设计，非论文） |
| 5 | 论文未给复数域求根的收敛判据数值（tol、最大迭代次数等） | 能补：沿用 Fig3 case 的经验判据（残差 $<10^{-8}$ 级别断言），复数域可能需要放宽或加密，需 step04 实测决定 | trust（教材惯例） |

## 3. 阶段性范围外（论文有但本 case 不复现，记录备查）

| # | 项 | 说明 |
|---|----|------|
| 6 | Fig7/Fig8（真实材料 Ag/Si 吸收谱，受 $\sigma^{\rm sa}_{{\rm abs},l}$ 限制） | 与 Fig6 有物理关联（超吸收上限的实测印证）但非本 case 直接目标；若 main-agent 后续想做交叉验证扩展，需另开 case 或加做项 |
| 7 | 第 4 节局限性修正（$\alpha_l,\beta_l$、过渡层等） | 非本 case 范围 |

## 4. 需人工/外部才能拿的信息

| # | 项 | 判断 |
|---|----|------|
| 8 | GUI 模板 / COMSOL image | **不需要**。本 case 纯解析/数值求根，不涉及 COMSOL/Magnus |
| 9 | 实验数据 | **不需要**。本文为纯理论/分析论文，无实验；Fig6 本身也是纯理论 loci，零材料依赖 |

---

## 总结判断

- **无硬阻塞公式/理论缺失**：Fig6 所需的全部公式（$a_l^{(2)}$、超吸收条件、吸收上限）均已在 tex 源中明确给出并转写进 formulas.md，来源清楚。
- **唯一潜在阻塞是技术实现细节**（#3，scipy 复数宗量支持），建议作为 step04 第一步验证项，而非现在的 step02 阻塞项——这是"实现是否可行"的问题，不是"论文信息是否缺失"的问题。
- **求根算法本身论文未给出具体数值方案**（#4/#5 均是本 case 自行设计的建议，需 step03/04 阶段由 main-agent/后续 sub-agent 评审决定是否采纳），这是从 Fig3（论文给出了隐含的清晰实数域方法：优化每个 $q_e$ 使 $a_l=1$ 或 $0$）到 Fig6（论文只说"optimized"、未给算法细节）的关键信息落差，**需要在 gate 环节明确告知 main-agent/用户**：Fig6 的数值方法是本 case 团队自行设计，非论文直接给出，复现的"参照物"是最终结果曲线形状是否与 Fig6 吻合，而非某个论文规定的具体算法步骤。
- **坐标轴精确范围/精确分支计数**（#1/#2）留待 step04（求根出结果后）和 step08（数字化对比时）精确化，当前 pipeline_completed 阶段的目视估读已经足够支撑 step03 设计决策。

## provenance
- source_artifact: arXiv 2401.04146（Text-rev.tex §3.3）+ 本 case figures.md/paper_understanding.md + case 0703-01 已有产物（交叉参考求根方法论差异）
- evidence_type: 论文原文核对 + 图像目视估读（部分待精确化，已如实标注）
- timestamp_version: 20260709
- scope_applicability: 本 case（0707-02）Fig6 超吸收态复现的输入完备性判断
- confidence_result_class: 中高（公式/理论层完整，实现细节和坐标轴精确值待补）/ pipeline_completed
